{
  "name": "broadcast-studio",
  "version": "1.0.0",
  "description": "Pro Broadcast Studio Mobile App with USB OTG Capture Card and WebRTC support",
  "main": "index.html",
  "scripts": {
    "build:www": "node build-www.js",
    "cap:add": "cap add android",
    "cap:sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
